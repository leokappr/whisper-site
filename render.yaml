services:
  - type: web
    name: whisper-site
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run whisper_site.py --server.port=$PORT --server.address=0.0.0.0"


